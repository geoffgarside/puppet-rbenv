case "$PATH" in
*$HOME/.rbenv/bin*) ;;
*)
	export PATH=$HOME/.rbenv/bin:$PATH
	;;
esac

case "$PATH" in
*$HOME/.rbenv/shims*) ;;
*)
	export PATH=$HOME/.rbenv/shims:$PATH
	;;
esac

eval $(rbenv init -)
